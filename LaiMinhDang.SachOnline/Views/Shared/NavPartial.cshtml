<nav class="navbar navbar-inverse">
    <div class="container">

        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed"
                    data-toggle="collapse" data-target="#navbar1">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="@Url.Action("Index","SachOnline")">
                SachOnline
            </a>
        </div>

        <div class="collapse navbar-collapse" id="navbar1">

            <!-- MENU TRÁI -->
            <ul class="nav navbar-nav">
                <li class="active">
                    <a href="@Url.Action("Index","SachOnline")">TRANG CHỦ</a>
                </li>
                <li><a href="#">GIỚI THIỆU</a></li>
                <li><a href="@Url.Action("LienHe","SachOnline")">LIÊN HỆ</a></li>
            </ul>

            <!-- MENU PHẢI -->
            <ul class="nav navbar-nav navbar-right">
                @Html.Action("LoginLogout", "SachOnline")
                @Html.Action("GioHangPartial", "GioHang")
            </ul>

            <!-- TÌM KIẾM -->
            <form class="navbar-form navbar-right" role="search" action="@Url.Action("KetQuaTimKiem", "SachOnline")" method="get">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Tìm kiếm" name="data" required>
                </div>
                <button type="submit" class="btn btn-default">Tìm</button>
            </form>

        </div>
    </div>
</nav>

